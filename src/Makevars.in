SNAP = snap-0.4
PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS) -I$(SNAP)/include @USE_MPI@

PKG_LIBS = @PKG_LIBS_MPI@ $(SHLIB_OPENMP_CFLAGS) $(SNAP)/lib/.libs/libsnap.a

.PHONY: all snap

all: $(SHLIB)
$(SHLIB): snap

snap:
	(cd snap-0.4; ./configure @SNAP_OPENMP@ CC="$(CC)" LIBS=@LRT_LIBS@ ; make)

